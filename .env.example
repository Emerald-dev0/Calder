# Avenor — Environment Variables
# Copy to .env and fill in values. Never commit secrets.

# ── Application ──────────────────────────────────────────────
NODE_ENV=development
LOG_LEVEL=info
# APP_URL=http://localhost:3000
# Founder emails (comma-separated) — auto-granted owner of the Avenor org on first OAuth login
# FOUNDER_EMAILS=you@example.com
# DASHBOARD_URL=http://localhost:3001
# API_URL=http://localhost:3002

# ── Database ─────────────────────────────────────────────────
DATABASE_URL=postgresql://calder:calder@localhost:5432/calder

# ── Redis (queue / cache / rate-limit) ───────────────────────
REDIS_URL=redis://localhost:6379

# ── Auth ─────────────────────────────────────────────────────
# AUTH_SECRET must be 32+ characters in production
AUTH_SECRET=dev-secret-change-me-32-chars-min
# BETTER_AUTH_URL or AUTH_URL depending on auth provider
AUTH_URL=http://localhost:3001

# ── Email Provider (AWS SES) ─────────────────────────────────
# Required in production: without credentials the sending path refuses to boot
# rather than silently simulating delivery. In development the mock provider
# records sends without delivering anything, which is the correct behaviour.
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# SES_FROM_DOMAIN=calder.click
# Sender for Calder's own mail (verification codes, magic links, receipts). SES
# sandbox only delivers from a verified identity, so this stays overridable
# without a deploy. Defaults to: Calder <hello@calder.click>
# AUTH_EMAIL_FROM=Calder <hello@calder.click>

# ── Billing (Bachs) ──────────────────────────────────────────
# BACHS_API_KEY=
# BACHS_WEBHOOK_SECRET=
# BACHS_API_URL=https://api.bachs.example.com

# ── Admin (broadcasts, interventions — unset disables /v1/admin/*) ──
# ADMIN_API_KEY=use-openssl-rand-hex-32-min-16-chars

# ── Webhooks ─────────────────────────────────────────────────
WEBHOOK_SIGNING_SECRET=whsec_dev_secret_change_me

# ── Encryption ───────────────────────────────────────────────
# For future use: encrypting sensitive fields at rest
# ENCRYPTION_KEY=32-byte-hex-key
